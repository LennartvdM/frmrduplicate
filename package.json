{
  "name": "neoflix-site",
  "version": "1.0.0",
  "private": true,
  "description": "Root manifest for the Netlify Functions in netlify/functions. The site itself is built from frankenstein/ — see build.sh.",
  "dependencies": {
    "@netlify/blobs": "^10.7.12"
  }
}
