/**
 * Toolbox page: Questions to Ask During Previewing
 * Thin wrapper — all logic lives in toolbox-page-factory.mjs
 */
import { createToolboxPage } from "./toolbox-page-factory.mjs";

var { page, metadata } = createToolboxPage("/Toolbox_Questions_to_ask_during_previewing", "Questions to Ask During Previewing");

export default page;
export { metadata as __FramerMetadata__ };
